// في themes.dart — أضف الألوان دي
import 'dart:ui';

class AppColors {
  // ── Dark ──
  static const darkBg      = Color(0xff0D0D12);
  static const darkSurface = Color(0xff1C1C1E);
  static const darkBorder  = Color(0xff2a2a3a);

  // ── Light ──
  static const lightBg      = Color(0xffF5F5F7);
  static const lightSurface = Color(0xffFFFFFF);
  static const lightBorder  = Color(0xffE0E0E0);

  // ── Shared ──
  static const primary = Color(0xff77094E);
}